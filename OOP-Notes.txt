>>>> Basics of Objects and Classes!
.
.
.
import java.util.Arrays;
public class Solution {
   public static void main(String[] args) {
   Student std1 = new Student();

   std1.name = "Zakee ahmad";
   std1.age = 21;
   std1.marks = 9f;

   System.out.println(std1.name);
   System.out.println(std1.age);
   System.out.println(std1.marks);
   }
   
  static class Student{
      String name;
      int age;
      float marks;
   }
}

>>>> Access the Class data via using ArrayList At one time;
.
.
.
import java.util.ArrayList;
public class Main {
    public static void main(String[] args) {
        Student std1 = new Student();
        std1.age = 21;
        std1.name = "Zakee ahmad";
        std1.marks = 98;

        ArrayList<Student> list = new ArrayList<>();
        list.add(std1);
        for (Student i : list) {
            System.out.println("Name is : " + i.name + "| "+"Age is : " + i.age + "| "+"Marks's is : "+ + i.marks);
        }
    }
    static class Student {
        String name;
        int age;
        float marks;
    }
}

>>>> This is all about constructor !
.
.
.
import java.util.Arrays;
public class Solution {
   public static void main(String[] args) {
   Student std1 = new Student(); // We can Create Multiple objects by using same tamplate.
   Student std2 = new Student();
   System.out.println(std2.name);
   System.out.println(std2.age);
   System.out.println(std2.marks);
   }
  static class Student{
      String name;
      int age;
      float marks;
      
      Student(){  // This is our constructor 
         this.name ="Ahmad_Zakee...";
         this.age = 21;
         this.marks = 9f;
      }
   }
}

>>>> We can create a function inside our class and access that function by using the objects !
.
.
.
public class Main {
    public static void main(String[] args) {
        Student std1 = new Student();
        std1.Greet("Zakee ahmad"); // Run this code & see output :
        std1.Student_Data("Zakee ahmad", 21, 98f);
        int fact = std1.Factorial(5);
        System.out.println("Factorial of the number is : " + fact); // Output : 120

        Student std2 = new Student();  // We can create multiple objects by using same tamplate.
        std2.Greet("Gaush alam"); // std2 also return the same output
    }

    static class Student {
        String name;
        int age;
        float marks;

        Student() {
            this.name = "Zakee ahmad";
            this.age = 21;
            this.marks = 98f;
        }

        void Greet(String name) { // Void function its not return any value
            System.out.println("Have a nice day! " + name);
        }

        void Student_Data(String name, int age, float marks) { // This function pass all the data from Student
            System.out.println("Name : " + name + "\n" + "Age : " + age + "\n" + "marks : " + marks);
        }

        int Factorial(int n) { // This function return the the data!
            int fact = 1;
            for (int i = 1; i <= n; i++) {
                fact *= i;
            }
            return fact;
        }
    }
}

>>>> Another object with assigning Different value !
.
.
public class Main {
    public static void main(String[] args) {
        Student std1 = new Student(); // Every Objects consists a default constructor
        System.out.println(std1.name); // Output : Zakee ahmad

        Student std2 = new Student("Gaush alam", 20, 100f);
        System.out.println(std2.name); // Output : Gaush alam
    }

    static class Student {
        String name;
        int age;
        float marks;

        Student() {   // When we are not passing any value then this constructor will be called!
            this.name = "Zakee ahmad";
            this.age = 21;
            this.marks = 98f;
        }

        Student(String name ,int age ,float marks) { // If we are passing referenc values then this counstructor will be called!
            this.name = name;
            this.age = age;
            this.marks = marks;
        }
      
    }
}

>>>> WAP to create a class of 4 students with there properties!
.
.
public class Main {
    public static void main(String[] args) {
        Student std1 = new Student("Zakee ahmad", 21, 95f);
        Student std2 = new Student("Gaush Alam", 22, 97f);
        Student std3 = new Student("Waris Alam", 22, 96f);
        Student std4 = new Student("Md Haris khan", 23, 99f);
        
        System.out.println(std1.name); // Output : Zakee ahmad
        System.out.println(std2.name); // Output : Gaush alam
        System.out.println(std3.name); // Output : Waris alam
        System.out.println(std4.name); // Output : Md haris khan
        
    }

    static class Student {
        String name;
        int age;
        float marks;

        Student(String name, int age, float marks) { // This is our constructor!
            this.name = name;
            this.age = age;
            this.marks = marks;
        }
    }
}

>>>> New Objects can be inharent the properties of another objects!
.
.
public class Main {
    public static void main(String[] args) {
        Student other = new Student();
        System.out.println(other.name); 
        Student std = new Student();
        System.out.println(std.name);         
    }
    static class Student {
        String name;
        int age;
        float marks;
        Student() {
            name = "Zakee ahmad";
            age = 21;
            marks = 98f;
        }

        // Student std1 = new Student("Gaush alam ,21,99f");
        // Here this will replace with std1
        Student(String name, int age, float marks) { // This is our constructor!
            this.name = name;
            this.age = age;
            this.marks = marks;
        }

        // Same thing is working internally
        Student (Student other) {
            this.name = other.name;
        }
    }
}

>>>> Change the name of the Student.
.
.
public class Main {
    public static void main(String[] args) {
        Student std = new Student();
        System.out.println(std.name);
        std.changeName("Waris alam"); // We can change the name via Function!
        System.out.println(std.name); // Now this will return : Waris alam
    }

    static class Student {
        String name;
        int age;
        float marks;

        Student() { // Same thing is done below!
         this("Zakee ahamd",21,98f);
        }

       /* Student() { 
            name = "Zakee ahmad";
            age = 21;
            marks = 98f;
        }  */

        Student() {
         this.name = name;
         this.age = age;
         this.marks = marks;
        }


        void changeName(String name) {
            this.name = name;
        }
    }
}

>>>> Day = 03 (Principles - Inheritence ,polymorphism ,Encapsulation ,Abtraction!)

:: This is Our main class!

public class Solution{
    public static void main(String[] args) {
    Parent std = new Parent();

    Parent child1 = new Parent(1000000, 3, 2, "Hurari");
    System.out.println(child1.carName);
    System.out.println(child1.money);
    child1.Blashing();

    Parent child2 = new Parent();
    System.out.println(child2.carName);

    Child Ahmad = new Child();
    System.out.println(Ahmad.Name); 
    System.out.println(Ahmad.Education);
    
    Child Zakee = new Child("Zakee ahmad", 21, 21000000.00, "B.Tech");
    System.out.println(Zakee.Education);
    System.out.println(Zakee.ownMoney);

    Child PtyOfPrntCls = new Child(10000000, 3, 1, "B-M-W", "Zakee ahmad", 21, 210000000.00, "Computer-Sceince-Student");
    System.out.println(PtyOfPrntCls.Education);
    System.out.println(PtyOfPrntCls.money); // It will not be access those members of parent class that have been decleared in parent class as privet.
                                           //  Remember this thing!
    }
}



:: This is our Parent class:

public class Parent {
    float money;
    int bungla;
    int car;
    String carName;

     Parent() {
        money = 100000000000f;
        bungla = 3;
        car = 2;
        carName = "B-M-W";
    }

     Parent(float money, int bungla, int car, String carName) {
        this.money = money;
        this.bungla = bungla;
        this.car = car;
        this.carName = carName;
    }

     Parent(Parent old){
        this.bungla = old.bungla;
        this.carName = old.carName;
        this.car = old.car;
        this.money = old.money;

    }

     void Blashing() {
        System.out.println("May allah bless You! we are allways with you my child 🤍🥹 ");
    }

    
}

:: Now a child inharent the properties of its Parent class & that all things are accesed in above main clas via creating objects!

public class Child extends Parent {
    String Name;
    int age;
    Double ownMoney;
    String Education;

     Child(String Name, int age, Double ownMoney, String Education) {
        this.Name = Name;
        this.age = age;
        this.ownMoney = ownMoney;
        this.Education = Education;
    }

     Child() {
        this.age = 21;
        this.ownMoney = 2100000000000.00;
        this.Education = "Engineer";
        this.Name = "Zakee ahmad";
    }
    
    Child(float money,int bungla,int car,String carName ,String Name, int age, Double ownMoney, String Education) {
        super(money, bungla, car, carName);
        this.Name = Name;
        this.age = age;
        this.ownMoney = ownMoney;
        this.Education = Education;
    }
    
}

>>> We can Access both Parent & Child address at the same time ,
 Via using super keyword super keyword refer to the Parent class and other side
  this keyword refer to the oun class Objects :)

  
class Parent{
    String fatherName;
    String motherName;
    String address;

    Parent(){
        fatherName = "Md Noushad Alam";
        motherName = "Nusrat parween";
        address = "Punjab , Pakistan , jammu kashmir";
    }
}
class Child1 extends Parent{
         String name;
         int age;
         String address;
  
         Child1(){
            super();
            this.address="Dih-Sarsauna,Tajpur,Samastipur,Bihar";
            age = 21;
            name = "Zakee ahamd";
         }
         void ParentAdd(){
            System.out.println(super.address);
         }

}

public class Solution{
    public static void main(String[] args){
        Child1 ch1 = new Child1();
        System.out.println(ch1.address);
        ch1.ParentAdd();
}
}

>> Multilevel Inheritence ?

class Grand_Parent{
    String grandName;
    String address;
    
     Grand_Parent(){
        grandName = "Sayda khatoon";
        address = "London , New york,Zarmany";
}
}

class Parent extends Grand_Parent{
    String fatherName;
    String motherName;
    String address;

    Parent(){
        fatherName = "Md Noushad Alam";
        motherName = "Nusrat parween";
        address = "Punjab , Pakistan , jammu kashmir";
    }

    void GrandAdd(){
        System.out.println(super.address);
    }
}
class Child1 extends Parent{
         String name;
         int age;
         String address;
  
         Child1(){
            super();
            this.address="Dih-Sarsauna,Tajpur,Samastipur,Bihar";
            age = 21;
            name = "Zakee ahamd";
         }
         void ParentAdd(){
            System.out.println(super.address);
         }

}

public class Solution{
    public static void main(String[] args){
        Child1 ch1 = new Child1();
        System.out.println(ch1.address);
        ch1.ParentAdd();
        ch1.GrandAdd();

}
}

Polymorphism in OOP (Object-Oriented Programming)

Definition:
Polymorphism means “one name, many forms.” It allows the same method or function to behave differently depending on the object or context.

Key Idea:
A single interface can represent different types of behavior.

Types of Polymorphism

Compile-Time Polymorphism (Static Polymorphism)

Achieved using Method Overloading

Same method name but different parameters

class Calculator {
    int add(int a, int b) {
        return a + b;
    }

    int add(int a, int b, int c) {
        return a + b + c;
    }
}

Run-Time Polymorphism (Dynamic Polymorphism)

Achieved using Method Overriding

A child class changes the behavior of a parent class method.

class Animal {
    void sound() {
        System.out.println("Animal makes sound");
    }
}

class Dog extends Animal {
    void sound() {
        System.out.println("Dog barks");
    }
}


>>>> @Override The one class to another class !


class Traingle {
    void area() {
        System.out.println("I am in traingle!");
    }
}

class Shapes extends Traingle {
    void ara() {
        System.out.println("I'm in Shape!");
    }
}

class Square extends Shapes {
    @Override
    void area() {
        System.out.println("I'm in Square!");
    }
}

public class Main {
    public static void main(String[] args) {
        Square sr = new Square();
        sr.ara();
        sr.area();
    }
}

>>>>> Usecase of this keyword through out Override !

class Traingle {
    void area() {
        System.out.println("I am in traingle!");
    }
}

class Shapes extends Traingle {
    void ara() {
        System.out.println("I'm in Shape!");
    }

    float height;

    Shapes() {
        this.height = 122.23f;
    }
}

class Square extends Shapes {
     @Override
    void area() {
        System.out.println("I'm in Square!");
    }

    float height;

    Square() {
        this.height = 23.1f;
    }

    void ShapesHeight() {
        System.out.println(super.height);
        System.out.println(this.height);
    }
}

public class Main {
    public static void main(String[] args) {
        Square sr = new Square();
        // System.out.println(sr.height);
        sr.ShapesHeight();
    }
}


2️⃣ final Method

When a method is declared final,
👉 it cannot be overridden in a child class.

Example:
class Parent {
    final void show() {
        System.out.println("Hello");
    }
}

class Child extends Parent {
    void show() { }  // ❌ Error
}
📌 Why use it?

To prevent changing important logic

To maintain consistent behavior in inheritance

Interview line:

A final method prevents method overriding to protect business logic.

>>>>>  Abstraction  & Encapsulation !



🔹 1. Abstraction in Java
✅ Definition (Write this in exams)

Abstraction is the process of hiding implementation details and showing only essential features of an object.

👉 Focus: What an object does, not how it does it.

🔹 Real-Life Example

When you use an ATM machine, you can:

Withdraw money

Check balance

But you don’t know the internal banking server logic.

That is abstraction.

🔹 How Java Achieves Abstraction?

Java provides abstraction using:

Abstract Class

Interface

🔹 Example using Abstract Class
abstract class Animal {
    abstract void sound();   // abstract method (no body)

    void eat() {             // normal method
        System.out.println("Animal eats food");
    }
}

class Dog extends Animal {
    void sound() {
        System.out.println("Dog barks");
    }
}
🔹 Key Points

Abstract class → declared using abstract keyword.

Abstract method → no body.

Object of abstract class cannot be created.

Child class must implement abstract methods.

🔹 2. Encapsulation in Java
✅ Definition (Write this in exams)

Encapsulation is the process of wrapping data (variables) and methods into a single unit and restricting direct access to data.

👉 Focus: Data hiding + controlled access.

🔹 How Java Achieves Encapsulation?

Using:

private variables

Public getter and setter methods

🔹 Example
class Student {
    private int marks;   // data hiding

    public void setMarks(int marks) {
        this.marks = marks;
    }

    public int getMarks() {
        return marks;
    }
}
🔹 Why Encapsulation is Important?

✔ Protects data
✔ Improves security
✔ Makes code maintainable
✔ Allows validation before setting values

Example with validation:

public void setMarks(int marks) {
    if(marks >= 0 && marks <= 100) {
        this.marks = marks;
    }
}
🔥 Abstraction vs Encapsulation (Exam Comparison Table)
Feature	Abstraction	Encapsulation
Meaning	Hiding implementation	Hiding data
Focus	What to show	How to protect data
Achieved By	Abstract class / Interface	Private variables + getters/setters
Level	Design level	Implementation level
🎯 Interview-Ready One Line Answer

If interviewer asks:

What is the difference?

You say:

Abstraction hides implementation details and shows only essential features, while encapsulation hides data and restricts direct access using access modifiers.